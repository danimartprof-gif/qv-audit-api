# QV Audit API
Zero-dependency Node service. POST /api/audit scores an influencer (Vertex Gemini) and emails dirección (Gmail API).
Env: SA_JSON, GCP_PROJECT, GMAIL_CLIENT_ID, GMAIL_CLIENT_SECRET, GMAIL_REFRESH_TOKEN, AUDIT_RECIPIENTS, CORS_ORIGIN, SENDER_EMAIL.
